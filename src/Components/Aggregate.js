import React from "react";

const Aggregate = () => {
  const myStyles = {
    width: "40%",
    display: "inline-block"
  };
  return (
    <div style={myStyles}>
      <h2>Number and a Text</h2>
    </div>
  );
};

export default Aggregate;
